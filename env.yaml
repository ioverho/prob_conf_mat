name: bayes-conf-mat
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - numpy
  - scipy
  - matplotlib
  - seaborn
  - pandas
  # Optional for dev
  - ipykernel
  - scikit-learn
  - pip:
      - jaxtyping
      - tabulate
      - lightkde
      # Optional for dev
      - black
      - ruff
