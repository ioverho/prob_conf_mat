name: bayes-conf-mat
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - numpy
  - matplotlib
  - scipy
  # Try to remove
  - seaborn
  - pandas
  # Optional for dev
  - ipykernel
  - scikit-learn
  - pip:
      - jaxtyping
      - lightkde
      - strictyaml
      # Optional for dev
      - black
      - ruff
